<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.fjut.mybatis.mapper.RoleMapper">

    <sql id="select">
        SELECT id, role_name AS roleName, permissions FROM role
    </sql>

    <select id="findAll" resultType="Role">
        <include refid="select"/>
        ORDER BY id
    </select>

    <select id="findById" resultType="Role">
        <include refid="select"/>
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="Role" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO role (role_name, permissions) VALUES (#{roleName}, #{permissions})
    </insert>

    <update id="update" parameterType="Role">
        UPDATE role
        <set>
            <if test="roleName != null and roleName != ''">role_name = #{roleName},</if>
            <if test="permissions != null and permissions != ''">permissions = #{permissions},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM role WHERE id = #{id}
    </delete>

</mapper>
